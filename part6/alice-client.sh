#!/bin/bash

# 1a: set values of message x and shared secret s variables



# 2a: create a list of hash functions, iterate through them in a for loop



	# 3: generate client-side hash - pipe message into appropriate hash function, set variable contents to result of command, print variable to console
	
	
	
	# 4a: send message and then client-side hash to server - pipe variable contents into 2 connecting socket commands
	
	
	
	
